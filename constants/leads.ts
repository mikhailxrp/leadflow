/** Semantic filter value — means «no assignee», not a real user id. */
export const UNASSIGNED_MANAGER_ID = 'unassigned' as const;

export const DEFAULT_LEADS_PAGE_SIZE = 20;
export const MAX_LEADS_PAGE_SIZE = 100;
