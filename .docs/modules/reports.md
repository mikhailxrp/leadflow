# Модуль: Отчёты (reports)

> Спецификация минимальной аналитики: лиды по периоду, скорость ответа, конверсия, причины отказа, по сотрудникам, по источникам. Единый набор для всех компаний — деления по тарифу больше не существует. Построен полностью на существующих данных (`Lead`/`Event`/`Task`), без новых таблиц.
> Связанные файлы: `.docs/database.md`, `.docs/modules/leads.md` (переиспользуемый счётчик менеджера), `.docs/modules/risk.md`.

---

## Содержание

1. Цели модуля
2. Архитектурные решения
3. Состав отчётов
4. Соотношение со страницей «Контроль»
5. API-эндпоинты
6. Файлы, которые создаются
7. Серверные правила безопасности
8. Связи с другими модулями

---

## Цели модуля

После завершения этого модуля:

- Руководитель и Администратор видят: лиды по периоду, скорость первого ответа, конверсия по этапам, причины отказа, разбивку по сотрудникам и источникам
- Доступен экспорт в CSV
- Все цифры — производные от `events`/`Lead`/`Task`, без отдельной таблицы метрик и без дублирования логики, уже написанной для других экранов

**Не входит в модуль:**

- Доступ Менеджера к отчётам — отчёты остаются возможностью Руководителя и Администратора (раздел 3 `prd.md`), не Менеджера. Экран защищён как `(management)`, не как обычная рабочая зона (см. `CLAUDE.md`)
- Деление глубины отчётов по тарифу — тарифов не существует, набор один для всех компаний
- ROMI/выручка — в составе финансового блока (Phase 22.7, раздел «Финансовый блок» ниже); прогноз выручки, план/факт, атрибуция по дате закрытия (вместо когорты привлечения по `createdAt`) — по-прежнему вне scope
- Счётчик активности менеджеров как управленческий инструмент реального времени — это `/control` (`.docs/modules/leads.md`); здесь — тот же расчёт, но в составе общего отчёта за произвольный период, не отдельная логика

---

## Архитектурные решения

### Переиспользование, не дублирование

«По сотрудникам» в этом модуле — это `lib/control/getManagerStats.ts` (уже написан для `/control`, см. `.docs/modules/leads.md`), вызванный с другим диапазоном периода (`periodStart`/`periodEnd` вместо только «последние N дней») и в другом месте интерфейса. Площадка другая, расчёт — один и тот же. Дублировать формулы под новым именем — риск, что они разойдутся при будущих правках.

### Доступ — `requireCompanyAccess`, не прямая проверка роли

Отчёты доступны Руководителю, Администратору **и маркетологу** в режиме входа внутрь компании (allow-list, `constants/marketerAccess.ts`). У маркетолога нет `session.user` (только `session.marketer`) — прямая проверка `session.user.role` бросила бы исключение на его сессии. Поэтому каждый route handler использует единый guard, учитывающий оба типа company-актора:

```typescript
// GET /api/reports/* — каждый route handler
const actor = await requireCompanyAccess({
  minRole: "HEAD",
  method: "GET",
  pathname: "/api/reports/summary", // свой путь для каждого эндпоинта
});
// actor.companyId — для user и для marketer одинаково доступен
```

Раньше здесь была проверка тарифа (`requirePlan`) — убрана полностью вместе с биллингом.

---

## Состав отчётов

### Лиды по периоду

```
Сколько лидов поступило за период (группировка по дню/неделе для графика)
```

### Скорость первого ответа

```
Среднее время от LEAD_CREATED до LEAD_TAKEN_IN_WORK, за период
(лиды без LEAD_TAKEN_IN_WORK в периоде — не входят в среднее, но считаются отдельно как "необработанные")
```

### Конверсия по этапам

```
Для каждого этапа: сколько лидов сейчас в нём И сколько лидов когда-либо через него проходило (по STAGE_CHANGED)
Конверсия в финальную сделку = count(closeType=WON) / count(всех лидов за период)
```

Сколько осталось необработанных, сколько зависших, сколько без следующего действия (FR-150-2) — переиспользование `lib/risk/computeRiskBatch.ts` (`.docs/modules/risk.md`), агрегированное по периоду.

### Причины отказа

```
group by lossReasonId, count(Lead) where closeType = LOST, за период
```

### По сотрудникам

Переиспользует `getManagerStats()` — за произвольный период, не только «текущий месяц», как в `/control`.

### По источникам

```
group by source, count(Lead), conversion (closeType=WON / total), за период
```

### Экспорт CSV

Любой из вышеперечисленных отчётов — кнопка «Экспорт» → CSV с теми же данными, что на экране. Доступен всем, у кого есть доступ к самому отчёту — отдельного ограничения на экспорт не существует.

---

## Соотношение со страницей «Контроль»

| | `/control` | `/reports` |
| --- | --- | --- |
| Назначение | Операционное управление здесь и сейчас | Аналитика за период |
| Период | Текущий месяц по умолчанию | Произвольный, выбираемый |
| Доступ | Руководитель, Администратор | Руководитель, Администратор, маркетолог (allow-list) |
| Расчёт «по сотрудникам» | `getManagerStats()` | Тот же `getManagerStats()`, другой период/UI |

Это не два конкурирующих модуля — `reports.md` использует часть того же кода, что и `leads.md` → «Страница контроля», под другим экраном с другим назначением. Оба теперь живут в зоне `(management)`, не `(admin)` (см. `CLAUDE.md`).

---

## API-эндпоинты

| Метод | Путь | Назначение | Auth | Право |
| --- | --- | --- | --- | --- |
| GET | `/api/reports/summary` | Лиды по периоду, скорость ответа, конверсия, необработанные/зависшие/без действия | Session | Руководитель, Администратор, маркетолог |
| GET | `/api/reports/loss-reasons` | Причины отказа | Session | Руководитель, Администратор, маркетолог |
| GET | `/api/reports/by-employee` | По сотрудникам | Session | Руководитель, Администратор, маркетолог |
| GET | `/api/reports/by-source` | По источникам | Session | Руководитель, Администратор, маркетолог |
| GET | `/api/reports/export` | CSV-экспорт любого из отчётов | Session | Руководитель, Администратор, маркетолог |
| GET | `/api/reports/financial` | Финансовый блок: 10 метрик + ROMI за период (Phase 22.7) | Session | Руководитель, Администратор, маркетолог |
| GET | `/api/ad-spend` | Справочник расхода на рекламу по месяцам (весь список без `from`/`to`, или пересекающие диапазон — с ними) | Session | Руководитель, Администратор, маркетолог |
| POST | `/api/ad-spend` | Внести/изменить расход за месяц (upsert по `companyId+year+month`) | Session | Руководитель, Администратор, маркетолог |

### `GET /api/reports/summary`

**Request:** `GET /api/reports/summary?from=2026-06-01&to=2026-06-30`

**Response 200:**
```json
{
  "totalLeads": 248,
  "avgFirstResponseMinutes": 12,
  "unprocessed": 4,
  "stuck": 7,
  "withoutNextAction": 11,
  "conversionByStage": [ { "stageId": "...", "name": "Новый лид", "count": 248 } ],
  "wonRate": 0.18
}
```

---

## Файлы, которые создаются

```
app/
├── (management)/reports/page.tsx
└── api/
    ├── ad-spend/route.ts                # GET (список) + POST (upsert месяца), Phase 22.7
    └── reports/
        ├── summary/route.ts
        ├── loss-reasons/route.ts
        ├── by-employee/route.ts
        ├── by-source/route.ts
        ├── financial/route.ts           # 10 метрик + ROMI, Phase 22.7
        └── export/route.ts

components/
└── reports/
    ├── ReportsPage.tsx                 # переключатель периода + вкладки (+ «Финансы», + «Расходы на рекламу» без периода)
    ├── LeadsOverTimeChart.tsx
    ├── ResponseSpeedCard.tsx
    ├── StageConversionChart.tsx
    ├── LossReasonsChart.tsx
    ├── ByEmployeeTable.tsx
    ├── BySourceTable.tsx
    ├── AdSpendSection.tsx               # справочник расхода по месяцам, Phase 22.7
    ├── FinancialReportSection.tsx       # 10 карточек финансового блока, Phase 22.7
    └── ExportButton.tsx

lib/
├── adSpend/
│   └── monthRange.ts                   # yearMonthKey/isWithinRange — общая логика для ad-spend/route.ts и getFinancials.ts
└── reports/
    ├── getSummary.ts
    ├── getLossReasonsBreakdown.ts
    ├── getBySource.ts
    ├── getFinancials.ts                # 10 метрик финансового блока, Phase 22.7
    └── exportToCsv.ts
```

### Расход на рекламу (`AdSpend`, Phase 22.7)

Ручной справочник расхода по календарным месяцам (`companyId+year+month`, upsert) — источник метрики «потрачено с НДС» финансового блока (ниже). Не подтягивается из Яндекс Директа: ручной ввод работает для всех компаний и любых каналов. Доступ — тот же набор, что у остальных отчётов (`requireCompanyAccess`, HEAD+ и маркетолог); вкладка на `/reports` не привязана к переключателю периода отчёта — редактор показывает весь справочник компании. Подробности схемы — `.docs/database.md` → «Модель: Расход на рекламу (AdSpend)».

### Финансовый блок (Phase 22.7)

Вкладка «Финансы» на `/reports` — 10 показателей сквозной экономики рекламы за выбранный период (`GET /api/reports/financial?from=&to=`, `lib/reports/getFinancials.ts`):

| № | Метрика | Формула |
| --- | --- | --- |
| 1 | Потрачено с НДС | Σ `AdSpend.amountWithVat` по месяцам, попавшим в период |
| 2 | Всего лидов | `count(Lead)` где `createdAt ∈ [from,to]` |
| 3 | Цена лида | (1) / (2), при (2)=0 → `null` |
| 4 | Квал-лиды | `count(Lead)` из когорты (2) где `qualification = QUALIFIED` |
| 5 | Цена квал-лида | (1) / (4), при (4)=0 → `null` |
| 6 | Доля квал-лидов, % | (4) / (2) × 100, при (2)=0 → `null` |
| 7 | Выручка в работе | Σ `dealValueEstimated` по когорте (2) где `closeType IS NULL` |
| 8 | Выручка в кассе | Σ `dealValueFinal` по когорте (2) где `closeType = WON` |
| 9 | Общая выручка | (7) + (8) |
| 10 | ROMI, % | ((9) − (1)) / (1) × 100, при (1)=0 → `null` |

**Когорта — единая, по `Lead.createdAt`, не по `closedAt`.** Все лидовые метрики (2, 4, 7, 8) считаются на одной выборке — лидах, **созданных** в периоде, — а расход (1) берётся за тот же период. Так связка «потратили → привлекли этих лидов → заработали → ROMI» согласована по одной когорте привлечения: лид, закрытый WON позже периода своего создания, всё равно учитывается в выручке периода, в котором он был создан, а не периода закрытия.

**Деление на ноль → `null`, не `0`/`Infinity`.** Метрики 3, 5, 6, 10 — `null` при нулевом делителе (нет лидов/квал-лидов/расхода); UI и CSV показывают «—». Числитель, равный нулю при ненулевом делителе (например, `costPerLead = 0` при расходе 0₽ и наличии лидов), — валидное число, не `null`.

**Без двойного счёта в общей выручке.** «Выручка в работе» (7) скопирована по `closeType IS NULL`, не по «сумма не null»: у LOST-лида `dealValueEstimated` не зануляется (зануляется только при WON, см. `lib/leads/closeLead.ts`), поэтому фильтр именно по статусу закрытия. WON-лид даёт только «выручку в кассе» — `dealValueEstimated` зануляется в той же транзакции, что и запись `dealValueFinal` (Phase 22.7, Таск 1).

**Валюта — рубль, суммы денежные — через `Decimal`.** Никакого пересчёта НДС: `AdSpend.amountWithVat` и `Lead.dealValueFinal`/`dealValueEstimated` уже с НДС. Суммы складываются в `Prisma.Decimal` до единственной финальной конвертации в `Number` (не сложение уже сконвертированных чисел — риск float-дрейфа на денежных полях).

**Пресеты периода — месяц-выровненные.** Расход `AdSpend` гранулярен по календарному месяцу — при отчёте за частичный месяц засчитывается его полная сумма. Переключатель периода на `/reports` (общий для всех вкладок) включает «Этот месяц», «Прошлый месяц», «Квартал», «Год» — плюс произвольный диапазон (например, оставшийся «30 дней»), под который блок выводит подсказку о месячной гранулярности расхода.

**Экспорт CSV** — `GET /api/reports/export?report=financial` (`financialToCsv()`): агрегатный отчёт, поэтому CSV — построчно «Метрика/Значение» (10 строк), не список записей, как у остальных экспортов.

---

## Серверные правила безопасности

1. **Все эндпоинты `/api/reports/*` — `requireCompanyAccess({ minRole: "HEAD", method, pathname })`.** Для `user`-актора это `hasMinRole(role, "HEAD")`; для `marketer`-актора — сверка allow-list по пути и методу (`constants/marketerAccess.ts`). Менеджер не имеет доступа к отчётам ни в каком виде — ни как `user` (роль ниже HEAD), ни как маркетолог (allow-list не про роль компании), проверяется на сервере, не только скрытием пункта меню.
2. **`companyId` из актора** (`actor.companyId`, одинаково для `user` и `marketer`) — отчёты не пересекают тенантов.
3. **Экспорт — без чувствительных данных сверх того, что и так видно в отчёте** (без хэшей, токенов, внутренних идентификаторов сверх необходимого).

---

## Связи с другими модулями

- **`.docs/modules/leads.md`** — переиспользует `getManagerStats()`.
- **`.docs/modules/risk.md`** — переиспользует `computeRiskBatch()` для «необработанные/зависшие/без действия».
- **`.docs/database.md`** — модели `Lead`, `Event`, `Task`, `LossReason` — без новых сущностей.
